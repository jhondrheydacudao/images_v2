# Use Ubuntu as the base image
FROM ubuntu:latest

# Set the working directory inside the container
WORKDIR /app/data

# Set environment variables (you can change this as needed)
ENV START="echo 'Hello, Ubuntu!'"

# Default command to run the environment variable command
CMD ["sh", "-c", "$START"]
